# 记忆注入优化规划（2026-09-13）

> 前置阅读：`记忆系统深度优化分析报告.md`（8/10 规划）、`记忆系统优化分析报告.md`（8/19 修复记录）。
> 本文是对照当前代码落地情况后的**新一版规划**，并记录当日实施内容。

## 一、现状核对（规划 vs 实际）

8/10 报告的 P1"分层摘要未主动注入"**已实现**：`ContextManager::inject_historical_summaries`
（`src/agent/context.rs`）在 `Agent::new`（`src/agent/mod.rs`）时被调用，按 final → phase → round
预算加载，以 system 消息注入，链路是通的。

但落地后暴露 4 个新问题：

| # | 问题 | 证据 |
|---|------|------|
| 1 | 记忆无"长期/会话"分层：`.kb/summaries/default/` 是单层桶，6 周累积 528 文件 / 4.2MB（440 round + 87 phase + final） | 目录统计 |
| 2 | 注入预算 = max_tokens − system_prompt，几乎不设限，长会话挤占任务历史 | `inject_historical_summaries` 原实现 |
| 3 | final.md 聚合每次全量重读所有 phase（87 个 ≈ 4 万+ tokens/次 LLM 调用，慢且易爆输出上限） | `save_layered_summary` 原第 4 步 |
| 4 | 无遗忘机制、注入内容无时效/来源标注，模型易把过期上下文当事实 | 目录只增不减；消息头仅"【会话摘要】" |

注：`session_id` 硬编码 `"default"` 在旧设计下恰是跨会话召回能工作的前提（所有会话共享一个读桶）。
隔离 session_id 必须以"长期记忆晋升"稳定落地为前提，否则隔离=失忆——本期通过
"长期记忆区 + 旧桶回退"渐进迁移，**不动 session_id 语义**。

## 二、优化规划（分级）

**P0 记忆分层与晋升**（本期实施 ✅）
- 新增长期记忆区 `.kb/memory/entries/`（`src/agent/memory.rs::MemoryStore`）：
  - 晋升钩子挂在顶层 Agent 的 `finish`（`depth==0`），把任务总结晋升为带时间/来源的条目；幂等（与最新条目相同跳过）
  - 注入优先级：长期区非空 → 只注入长期区（单条 system 消息，含时效与冲突指引）；长期区为空 → 回退旧的分层摘要桶，保证升级不断档
  - 遗忘：条目超过 30 条删最旧

**P1 注入预算与标注**（本期实施 ✅）
- 注入预算独立上限：`max_tokens` 的 15%（`MEMORY_BUDGET_PERCENT`），两条路径统一受控
- 所有注入内容附"可能已过时、冲突以当前为准"指引与时间/来源标注
- final.md 改为**滚动增量聚合**（前情 final + 本阶段新增），聚合成本与历史阶段数无关

**P2 检索式注入**（未实施，下一步）
- 用首条用户消息对长期条目/阶段摘要做关键词打分，top-k 注入替代"时间倒序全塞"
- `SummaryStore` 维护 index.json（遗留 M2：528 文件全遍历 O(n)）

**P2 会话隔离**（未实施，须待 P0 稳定后）
- `session_id` 改启动生成（时间+PID），`--resume` 从状态文件恢复原 id；summaries 桶按会话隔离
- 旧 `default/` 桶做一次性归档或保留只读回退

**不建议做**：`MemoryProvider` trait 统一抽象（8/10 报告提出）——注入源目前仅摘要+条目两类，
等检索式注入落地后再收敛接口。

## 三、本期代码改动

| 文件 | 改动 |
|------|------|
| `src/agent/memory.rs` | **新增**：`MemoryStore`（save_entry / load_recent 预算挑选 / latest_body / prune）+ `render_entries` + 4 个单测 |
| `src/agent/mod.rs` | 注册 `memory` 模块；`finish` 处理点加顶层晋升钩子；`promote_to_long_term`；final.md 滚动增量聚合 |
| `src/agent/context.rs` | `MEMORY_BUDGET_PERCENT=15`；`inject_historical_summaries` 重写为"长期区优先、摘要桶回退"，统一预算与时效标注 |
| `src/agent/summary.rs` | `strip_frontmatter` 提升为 `pub(crate)` 供 memory 复用 |

验证：`cargo check` 通过；`cargo test agent::` 全绿；冒烟运行确认注入日志与晋升落盘。

## 四、运维注意

- 升级后首次运行：长期区为空，自动走摘要桶回退路径（行为与今天一致，仅预算收紧到 15%）
- 第一次任务 finish 后开始积累长期条目；`.kb/summaries/default/` 的 4.2MB 历史桶暂保留（回退数据源），确认长期路径稳定后可归档
- `--resume`/restart 行为不变：非空历史跳过注入
