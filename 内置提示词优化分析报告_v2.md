# 内置提示词优化分析报告 v2

> 分析时间：当前版本（基于 git log 最新的 `refactor(prompt): 全面重写系统提示词，修复 18 个问题` 之后）
> 分析范围：`src/prompt.rs`、`src/agent/identity.rs`、`src/agent/pipeline_stages.rs`、`src/agent/mod.rs`、`src/skills/mod.rs`、`src/tools/mod.rs`、`skills/code-review/SKILL.md`

---

## 总览

| 严重程度 | 数量 | 说明 |
|---------|------|------|
| 🔴 P0 | 0 | 可导致行为异常的矛盾 |
| 🟠 P1 | 3 | 明显问题，影响功能或效率 |
| 🟡 P2 | 4 | 应改进但不紧急 |
| 🟢 P3 | 2 | 小优化建议 |

---

## ✅ 已修复的问题（来自上一轮分析）

| 问题 | 优先级 | 状态 |
|------|--------|------|
| 双重标题（format_skills_for_prompt 与 build_system_prompt） | P0 | ✅ 已修复 |
| code-review 技能 finish 矛盾指令 | P0 | ✅ 已修复 |
| General 身份提示词过简 | P1 | ✅ 已修复 |
| Architect 缺少 edit_file 工具 | P1 | ✅ 已修复 |
| 构建目录列表不完整 | P2 | ✅ 已修复（补充了 __pycache__/、.venv/ 等） |
| finish 决策树不完整 | P2 | ✅ 已修复（5 步决策树 + 分析/多步骤分支） |
| format_skills_for_prompt 测试断言宽松 | P2 | ✅ 已修复（改为精确断言） |

---

## 🟠 P1: 重要问题

### 1. `read_symbol` 工具在子代理注册表中缺失

**位置**：`src/tools/mod.rs` 的 `new_subagent_registry()` 和 `new_subagent_registry_with_identity()`

**问题**：`read_symbol` 工具已注册到主 Agent 的 `register_builtin_tools()` 中，且在主系统提示词的"特殊工具说明"中也有描述。但在子代理的工具注册表中完全缺失：

- `new_subagent_registry()` 中注册的工具有 `read_file`、`batch_read_files` 等，但没有 `read_symbol`
- `new_subagent_registry_with_identity()` 中 `all_tool_names` 数组也不包含 `read_symbol`

**影响**：子代理（通过 `spawn_subagent` 或 pipeline 阶段创建）无法使用 `read_symbol` 精确定位符号定义，只能退而使用 `read_file` 读取整个文件，浪费 token。

**修复方案**：在 `new_subagent_registry()` 的注册列表和 `new_subagent_registry_with_identity()` 的 `all_tool_names` 数组中添加 `"read_symbol"`。

### 2. 子代理身份 `default_tools()` 不包含 `context_budget`/`compress_context`/`save_summary`

**位置**：`src/agent/identity.rs` 中所有 6 种身份的 `default_tools()`

**代码**：
```rust
// 以 Architect 为例，其他身份类似
Self::Architect => HashSet::from([
    "read_file", "batch_read_files", "write_file", "edit_file",
    "glob", "list_directory", "file_exists", "exec_command",
    "kb_store", "kb_query", "finish",
    // 缺少 context_budget, compress_context, save_summary
]),
```

**问题**：虽然 `new_subagent_registry()`（用于 `spawn_subagent` 的子代理）注册了这 3 个工具，但 `new_subagent_registry_with_identity()`（用于 pipeline 阶段的子代理）通过 `default_tools()` 过滤工具列表，而 `default_tools()` 不包含它们，导致 pipeline 子代理无法使用上下文预算管理工具。

**影响**：Pipeline 子代理（Architect/Implementer/Reviewer/Tester/Debugger/General）无法感知和管理自己的上下文预算，可能在复杂任务中因上下文耗尽而失败。

**修复方案**：为所有身份的 `default_tools()` 添加 `context_budget`、`compress_context`、`save_summary`。

### 3. `read_symbol` 和 `context_budget` 等工具在子代理身份提示词中缺失

**位置**：`src/agent/identity.rs` 的 `tool_display_order()` 和 `tool_list()`

**问题**：由于 `tool_display_order()` 不包含 `read_symbol`、`context_budget`、`compress_context`、`save_summary`，且 `default_tools()` 也不包含它们，所以子代理身份提示词中的"可用工具"列表不包含这些工具。子代理即使实际上有这些工具（通过 `new_subagent_registry`），也不知道它们的存在。

**影响**：子代理不知道可以用 `context_budget` 检查上下文使用情况，也不知道可以用 `read_symbol` 精确定位符号。

**修复方案**：在 `tool_display_order()` 中添加 `read_symbol`、`context_budget`、`compress_context`、`save_summary`，并在 `default_tools()` 中添加它们。

---

## 🟡 P2: 应改进

### 4. 主系统提示词"特殊工具说明"中缺少 `context_budget` 等工具的说明

**位置**：`src/prompt.rs` 的"特殊工具说明"部分

**问题**：主系统提示词有完整的"上下文预算管理"小节，详细说明了如何使用 `context_budget`、`compress_context`、`save_summary` 三个工具。但在"特殊工具说明"部分中，这些工具完全没有被列出（不像 `spawn_subagent`、`exec_command`、`read_symbol` 等有专门的说明）。

这导致 LLM 的认知不一致：上下文预算管理部分说"可以使用 context_budget 工具"，但特殊工具说明中找不到这个工具。

**影响**：低。LLM 通过 API 的 tool schema 知道这些工具的存在，但提示词结构不完整。

**修复方案**：在"特殊工具说明"部分添加 `context_budget`、`compress_context`、`save_summary` 的简要说明。

### 5. 流水线阶段模板 `{finish}` 占位符替换文本冗长重复

**位置**：`src/agent/mod.rs` 的 `finish_warning()` 函数

**代码**：
```rust
fn finish_warning(output_desc: &str, example_summary: &str) -> String {
    format!(
        "✅ 完成所有上述工作后，请调用 `finish` 工具终止本阶段，\n\
         并将{}摘要作为 `summary` 参数传入，例如：\n\
         `finish(summary=\"{}\")`。",
        output_desc, example_summary
    )
}
```

**问题**：此函数生成约 80 字的提示文本，6 个阶段各替换一次，总共约 480 字。其中"完成所有上述工作后，请调用 `finish` 工具终止本阶段"这部分在 6 个阶段中完全重复。

**影响**：中等。每个阶段模板增加约 80 字，对上下文预算有轻微影响。

**修复方案**：精简为 `"完成工作后调用 finish 工具终止本阶段，summary 参数格式：finish(summary=\"{example}\")"`，约 40 字。

### 6. 流水线后续阶段模板仍包含 `{task_ref}` 原始任务描述

**位置**：`src/agent/pipeline_stages.rs` 中阶段 4（审查）、5（修复）、6（记录）的模板

**问题**：后续阶段（如"问题修复"、"进度记录"）的任务模板仍然包含完整的 `{task_ref}` 替换。这些阶段只需要知道前一阶段的输出即可，完整的原始任务描述是冗余的。

例如，阶段 5 "问题修复"的模板：
```
这是流水线第五阶段：问题修复。

原始需求：
{task_ref}

上一阶段输出（审查报告）：
{context}
```

而阶段 5 只需要知道"审查报告发现了什么问题"即可，不需要完整的原始需求。

**影响**：低。每个后续阶段重复 ~100 字原始任务描述，5 个阶段共 ~500 字。

**修复方案**：阶段 3（测试）之后的模板去掉 `{task_ref}`，只保留 `{context}`。

### 7. `shared_guide()` 内容过于简洁

**位置**：`src/agent/identity.rs` 的 `shared_guide()` 函数

**当前内容**（仅 78 字符）：
```
通用规则
工具：kb_query→batch_read_files→kb_store→finish
输出：finish(summary=...)含①完成内容②关键发现③修改文件列表
终止：批量一次读完、已读不重读、不写中间文件、完成即finish
```

**问题**：这个指南非常简短，用符号链接的方式表达工具优先级，可能不够清晰：
- "工具：kb_query→batch_read_files→kb_store→finish" 箭头链的含义不够直观
- "终止"规则过于严格："不写中间文件"在某些场景下不合适（如大型报告需要写入文件）
- 缺少"完成条件"的说明：什么情况下可以调用 finish

**影响**：中等。子代理可能对工具使用优先级和完成条件理解不准确。

**修复方案**：扩充为更清晰的文本描述，明确工具优先级逻辑和完成条件。

---

## 🟢 P3: 小优化

### 8. `tool_display_order()` 与 `new_subagent_registry` 中实际注册的工具不一致

**位置**：`src/agent/identity.rs` 的 `tool_display_order()` 与 `src/tools/mod.rs` 的 `new_subagent_registry()`

**`tool_display_order()` 列表**：`read_file, batch_read_files, write_file, edit_file, exec_command, glob, list_directory, file_exists, kb_store, kb_query, finish`

**`new_subagent_registry()` 注册列表**：以上 + `context_budget, compress_context, save_summary`

**问题**：`tool_display_order()` 缺少 `context_budget`、`compress_context`、`save_summary`，导致 `tool_list()` 生成的工具列表不包含这些工具。但 `new_subagent_registry()` 实际上注册了它们。

**修复方案**：在 `tool_display_order()` 中添加这三个工具（或至少 `context_budget`）。

### 9. code-review 技能中的语言配置表格与主提示词构建目录列表有冗余

**位置**：`skills/code-review/SKILL.md` 的"语言特定配置参考"表格

**问题**：主系统提示词的安全策略中已经包含了完整的构建目录列表（`target/`、`node_modules/`、`__pycache__/` 等），而 SKILL.md 中又有一个"各语言构建目录"表格。虽然表格更详细（包含各语言的包管理器和配置文件），但构建目录部分的信息是重复的。

**影响**：极低。信息冗余但无害。

**修复方案**：可选 — 将 SKILL.md 中的构建目录列改为"参考主系统提示词的安全策略"，减少重复维护。

---

## 建议修复优先级

### 立即修复（P1）

| 问题 | 修复难度 | 修改文件 | 建议方案 |
|------|---------|---------|---------|
| read_symbol 子代理缺失 | 低 | `src/tools/mod.rs` | 在 `new_subagent_registry()` 和 `all_tool_names` 中添加 `"read_symbol"` |
| default_tools 缺少预算工具 | 低 | `src/agent/identity.rs` | 6 种身份的 `default_tools()` 中各添加 3 个预算工具 |
| tool_display_order 缺少工具 | 低 | `src/agent/identity.rs` | 添加 `read_symbol`、`context_budget`、`compress_context`、`save_summary` |

### 计划修复（P2）

| 问题 | 修复难度 | 建议方案 |
|------|---------|---------|
| 特殊工具说明缺少预算工具 | 低 | 在 `prompt.rs` 中添加 context_budget 等工具的说明 |
| finish_warning 文本冗长 | 低 | 精简为约 40 字 |
| 后续阶段重复 task_ref | 低 | 阶段 3 之后去掉 `{task_ref}` |
| shared_guide 过于简洁 | 低 | 扩充为更清晰的文本描述 |

### 可选修复（P3）

| 问题 | 修复难度 | 建议方案 |
|------|---------|---------|
| code-review 表格冗余 | 低 | 改为引用主提示词规则 |
| tool_display_order 不一致 | 低 | 同步添加预算工具 |