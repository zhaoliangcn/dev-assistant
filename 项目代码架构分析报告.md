# 项目代码组织架构分析报告

## 项目: dev-assistant-rs

> 分析日期: 2026-07-18

---

## 架构总览

```
src/
├── main.rs              # CLI 入口（约 150 行，职责清晰 ✓）
├── app.rs               # 应用协调层（约 420 行，偏重）
│
├── agent/               # Agent 核心逻辑
│   ├── mod.rs           # Agent 结构体 + LLM 循环（约 290 行）
│   ├── context.rs       # 上下文管理器（约 250 行，负担过重）
│   └── display.rs       # UI 展示缓冲区（约 40 行 ✓）
│
├── llm/                 # LLM 多 Provider 层
│   ├── client.rs        # LlmClient（约 145 行 ✓）
│   ├── models.rs        # 数据结构定义（约 110 行 ✓）
│   └── provider/        # Provider 实现
│       ├── mod.rs       # Trait + 工厂
│       ├── openai.rs    # OpenAI 兼容 API
│       ├── anthropic.rs # Anthropic/Claude
│       └── ollama.rs    # Ollama 本地模型
│
├── security/mod.rs      # 安全策略（约 550 行，含工具安全 spec）
├── session/mod.rs       # 会话日志（约 250 行 ✓）
├── skills/mod.rs        # 技能发现与解析（约 215 行 ✓）
│
├── tools/
│   ├── mod.rs           # ToolRegistry（约 155 行 ✓）
│   ├── file/            # 文件工具（拆分合理 ✓）
│   ├── meta_tools.rs    # finish/restart
│   └── system_tools.rs  # exec_command
│
├── ui/
│   ├── mod.rs           # 终端 UI 渲染（约 160 行）
│   └── message_output.rs# output 实现（约 80 行）
│
├── utils/
│   ├── error.rs         # AppError 定义
│   ├── message_level.rs # MessageLevel 枚举
│   └── message_output.rs# MessageOutput trait
│
├── prompt.rs            # System prompt 构建
├── repl.rs              # Slash 命令分发
└── restart.rs           # 重启流程
```

---

## 高优先级问题

### 1. Box::leak 生命周期 Hack（app.rs:73）— 危险反模式

**问题**：`ToolRegistry<'a>` 的生命周期参数绑定到 `SecurityPolicy` 引用，导致 `app.rs` 必须用 `Box::leak` 将 `SecurityPolicy` 泄漏为 `'static`。

```rust
// app.rs:73
let security: &'static SecurityPolicy = Box::leak(Box::new(SecurityPolicy::new(...)));
```

**建议**：`ToolRegistry` 使用 `Arc<SecurityPolicy>` 替代生命周期参数，彻底消除 `Box::leak`。这样 `Agent<'a>` 也可以去掉生命周期参数，简化整个类型签名。

```rust
// tools/mod.rs
pub struct ToolRegistry {
    tools: HashMap<String, ToolDefinition>,
    working_dir: PathBuf,
    pub security: Arc<SecurityPolicy>,  // 不再有生命周期参数
}
```

---

### 2. app.rs 职责过重 — 混合了协调、UI、日志、重启逻辑

**问题**：`app.rs`（约 420 行）同时承担了：

- 组件组装（`build`）
- 会话状态恢复（`load_state_or_fresh`）
- 交互式 REPL 循环（`run_interactive`）
- 一次性消息处理（`run_once`）
- 消息送达与 UI 渲染
- 重启流程处理（`handle_restart`）

**建议**：将 `process_user_message` 和 `handle_restart` 提取到 `repl.rs`，或新建 `interaction.rs` 模块。`app.rs` 仅保留 `build` 和高层 `run` 调度。

---

### 3. security/mod.rs 混入了工具安全 Spec — 关注点分离不当

**问题**：`security/mod.rs`（550 行）本应是安全策略，但其中 `tool_security_spec` 函数与工具耦合过紧，定义了每个工具的安全校验规则。

```rust
fn tool_security_spec(tool_name: &str) -> Option<ToolSecuritySpec> {
    match tool_name {
        "read_file" | "edit_file" => Some(ToolSecuritySpec { ... }),
        "write_file" => Some(ToolSecuritySpec { ... }),
        // ...
    }
}
```

**建议**：将 `ToolSecuritySpec`、`PathValidation` 以及 `tool_security_spec` 函数移到 `tools/mod.rs` 或 `tools/spec.rs`，让安全层通过接口查询，而不是内联工具的细节。

---

### 4. ContextManager 身兼多职 — 违反单一职责

**问题**：`context.rs`（250 行）管理了：

- 对话历史存储
- Token 估算
- 上下文压缩
- 展示状态管理
- 状态序列化/反序列化
- 消息去重与过滤

**建议拆分**：

| 职责 | 建议模块 |
|------|----------|
| `history: Vec<LlmMessage>` + 增删改查 | `ConversationHistory` |
| `estimate_tokens()` | `TokenCounter` 工具类型 |
| `compress()` | `ContextCompressor` 策略 |
| `display: DisplayBuffer` | 已拆分到 `display.rs` ✓ |

---

### 5. 大量 #[allow(dead_code)] — 代码"僵尸"问题

**问题**：分布在数十处，包括：

| 文件 | 死代码项 |
|------|----------|
| `agent/mod.rs` | `AgentConfig`、`AgentResult` |
| `llm/models.rs` | `LlmConfig`（及全部字段） |
| `session/mod.rs` | `log_tool_call`、`log_tool_result`、`log_separator`、`path()` |
| `ui/message_output.rs` | `SilentMessageOutput` |
| `skills/mod.rs` | `Skill::body()` |
| `config/mod.rs` | `load_llm_config()` |
| `llm/client.rs` | `legacy_new()`、`new()` |

**建议**：使用 `cargo +nightly udeps` 检查，未被引用的直接删除。

---

### 6. 零测试覆盖

**问题**：整个项目没有任何 `#[cfg(test)]` 模块。

**关键逻辑需要测试**：

| 模块 | 测试重点 |
|------|----------|
| `security/mod.rs` | 路径校验、命令评估、`validate_path` 边界 |
| `agent/context.rs` | `compress`、`estimate_tokens`、去重 |
| `skills/mod.rs` | `parse_frontmatter`、`discover_skills` |
| `prompt.rs` | `build_system_prompt` 格式 |
| `tools/*` | 各工具 handler |

---

## 中优先级问题

### 7. ui/message_output.rs 与 utils/message_output.rs 同名文件造成混淆

**问题**：

- `utils/message_output.rs` — 定义 `MessageOutput` **trait**
- `ui/message_output.rs` — 定义 `UIMessageOutput` / `CliMessageOutput` **实现**

两个文件同名，查阅时容易混淆。

**建议**：将实现移到 `utils/message_output.rs`（与 trait 同文件），或将 `ui/message_output.rs` 重命名为 `ui/output_impls.rs`。

---

### 8. Agent 的 context 字段是 pub 的 — 内部状态随意暴露

```rust
// agent/mod.rs:42
pub struct Agent<'a> {
    pub context: ContextManager,  // pub 暴露
}
```

外部代码直接操作 `agent.context.display`、`agent.context.history` 等内部状态。

**建议**：改为私有并提供委托方法：

```rust
impl Agent {
    pub fn add_display_message(&mut self, ...) { ... }
    pub fn get_display_messages(&self) -> ... { ... }
    pub fn set_active_model(&mut self, name: &str) { ... }
}
```

---

### 9. 依赖管理问题

**未使用的依赖**：

```toml
axum = { optional = true }           # ❓ 没有使用
tokio-tungstenite = { optional = true } # ❓ 没有使用
rustyline = "14.0"                   # ❓ 实际未用（用的 std::io::stdin().read_line）
crossterm = "0.27"                   # ❓ 实际未用（用的原始 escape code）
anyhow = "1.0"                       # ❓ 与 thiserror 重复
async-stream = "0.3"                 # ❓ 没有使用
```

**建议**：清理未使用的依赖。`anyhow` 和 `thiserror` 一般二选一（本项目中 `thiserror` 已够用）。

---

### 10. 终端渲染混合原始 Escape Code 与库

**问题**：`ui/mod.rs` 使用原始 `\x1b[2J\x1b[H` 等 ANSI 转义码，但项目同时依赖了 `crossterm`。两者之间应统一。

**建议**：要么全部用 `crossterm`（跨平台），要么移除 `crossterm` 依赖。

---

### 11. error.rs 中 is_rate_limited() 基于字符串模式匹配 — 脆弱

```rust
pub fn is_rate_limited(&self) -> bool {
    match self {
        AppError::RateLimited(msg) => {
            msg.contains("status 429") || msg.contains("Too Many Requests")
        }
        AppError::Llm(msg) => msg.contains("status 429") || ...
    }
}
```

**建议**：在 LLM provider 层返回结构化的 rate limit 错误，而不是靠字符串匹配。

---

### 12. SessionLogger 静默吞掉写入错误

```rust
writeln!(self.file, "...").ok();     // 错误被忽略
self.file.flush().ok();               // 错误被忽略
```

**建议**：至少用 `tracing::warn!` 记录写入失败。

---

## 低优先级 / 风格建议

### 13. repl.rs 过小（95 行）

只处理了 4 个 slash 命令（`/exit`, `/quit`, `/clear`, `/model`），可以合并到 `app.rs` 或 `interaction.rs`。

### 14. process_tool_calls 中 "finish" 特殊处理

`agent/mod.rs:196-212` 对 `finish` 做了硬编码的特殊判断。建议在 `ToolDefinition` 中加 `skip_security: bool` 标记，由 `ToolRegistry::execute` 内部根据标记决定是否走安全评估。

### 15. ToolArgs 和 ToolContext 非常薄

两者只包装了已有类型，可以考虑简化：

```rust
type ToolHandler = dyn Fn(&Value, &PathBuf) -> Result<ToolResult, AppError> + Send + Sync;
```

---

## 总结优先级表

| 优先级 | 问题 | 影响 |
|--------|------|------|
| 🔴 1 | `Box::leak` 生命周期 Hack | 内存泄漏、类型签名污染 |
| 🔴 2 | `app.rs` 职责过重 | 可维护性、可测试性 |
| 🔴 3 | 工具安全 Spec 在 security 模块 | 耦合、违反关注点分离 |
| 🔴 4 | `ContextManager` 身兼多职 | 难以测试和扩展 |
| 🔴 5 | 大量 dead code | 认知负担、维护成本 |
| 🔴 6 | 零测试覆盖 | 回归风险 |
| 🟡 7 | 同名文件混淆 | 可读性 |
| 🟡 8 | `Agent.context` 公开暴露 | 封装破坏 |
| 🟡 9 | 未使用的依赖 | 构建时间、二进制体积 |
| 🟡 10 | ANSI escape code 不一致 | 可维护性、可移植性 |
| 🟡 11 | 错误匹配靠字符串 | 脆弱性 |
| 🟡 12 | 日志写入静默吞错 | 数据丢失风险 |
| 🟢 13-15 | 其他风格/简化 | 代码整洁度 |

---

## 最值得优先动手的 3 件事

1. **消除 `Box::leak`** — 将 `ToolRegistry` 改为 `Arc<SecurityPolicy>`，去掉生命周期参数，波及面可控，回报最大
2. **拆分 `ContextManager`** — 拆出 `ConversationHistory` + `TokenCounter` + `ContextCompressor`
3. **清理 dead code + 无用依赖** — 删除 `#[allow(dead_code)]` 覆盖的代码和 `anyhow`/`rustyline`/`crossterm` 等未用依赖