# 内置提示词优化分析报告

> 分析范围：`src/prompt.rs`（主系统提示词）、`src/agent/identity.rs`（6 种 Agent 身份提示词）、`skills/code-review/SKILL.md`（技能提示词）、`src/agent/mod.rs`（流水线阶段模板）、`src/skills/mod.rs`（技能格式辅助）

---

## 总览

| 严重程度 | 数量 | 说明 |
|---------|------|------|
| 🔴 P0 | 2 | 可能引起行为异常或矛盾 |
| 🟠 P1 | 4 | 明显问题，影响效率或可维护性 |
| 🟡 P2 | 3 | 应改进但不紧急 |
| 🟢 P3 | 3 | 小优化建议 |

---

## 🔴 P0: 严重问题

### 1. 双重标题问题：`build_system_prompt()` 和 `format_skills_for_prompt()` 各自生成标题

**位置**：
- `src/prompt.rs:25` — `build_system_prompt()` 中：
  ```rust
  format!("\n## 可用技能\n\n以下技能已安装，根据任务选择使用：\n\n{}", skills_prompt.trim())
  ```
- `src/skills/mod.rs:199` — `format_skills_for_prompt()` 中：
  ```rust
  let mut sections = vec!["可用技能（请求匹配技能关键词时会自动激活，技能内容将附加到你的输入中）：".to_string()];
  ```

**问题**：当技能存在时，`format_skills_for_prompt()` 返回的字符串已经包含"可用技能"标题，而 `build_system_prompt()` 又加了一层 `## 可用技能` 标题。最终输出中会出现两个标题，结构混乱。**注意：该问题已在分析过程中修复**（`format_skills_for_prompt` 去掉标题，仅由 `build_system_prompt` 提供）。

**影响**：中等。LLM 通常能理解，但提示词结构不清晰。

**修复方案**：✅ **已修复** — `format_skills_for_prompt()` 不再输出标题，标题仅由 `build_system_prompt()` 提供。

### 2. code-review 技能与系统提示词关于"写中间文件"的矛盾指令

**位置**：`skills/code-review/SKILL.md` 第 81 行

**问题**：技能指令要求"审查结果直接通过 `finish` 的 summary 输出，不要写入 review-report.md 或其他文件"，但系统提示词的 Token 管理规则说"需要大量输出时，用 `write_file` 写入文件"。对于大型代码审查，finish(summary) 容量有限，不可能塞进全部内容。这导致 LLM 陷入两难。

**修复方案**：✅ **已修复** — 将规则改为"优先通过 finish 输出，如果结果太大（超过 2000 字符），可以写入文件并在 finish summary 中引用文件路径"。

---

## 🟠 P1: 重要问题

### 3. General 身份提示词过于简单

**位置**：`src/agent/identity.rs` 约第 267 行

**当前内容**（仅约 100 字）：
```
你是一个子代理。你的职责是完成父代理分配的任务。

工作方式：
1. 明确理解任务目标，如有疑问基于已有信息做出合理判断
2. 制定执行计划，按步骤推进，使用可用工具完成任务
3. 遇到工具失败时：① 阅读错误信息 ② 判断是临时错误还是永久错误...
4. 专注完成分配的任务，不要偏离范围
```

**问题**：与其他身份（~300-400 字）相比严重不足：
- 缺少**输出规范**：没有说明 finish 的 summary 应该包含什么字段
- 缺少**完成条件**：什么情况下可以调用 finish
- 缺少**沟通指南**：遇到需求不明确时怎么办
- 缺少**工具使用优先级**：不知道什么时候用 kb_query/kb_store

**影响**：较高。General 是流水线最终阶段（进度记录）和默认子代理类型，使用频率很高。

**修复方案**：✅ **已修复** — 扩充为包含职责、工作方式、输出要求、工具使用优先级的完整提示词。

### 4. 流水线阶段模板硬编码在 `run_pipeline()` 中

**位置**：`src/agent/mod.rs` 的 `run_pipeline()` 方法（约 200 行用于构建 6 个阶段模板）

**问题**：6 个 PipelineStage 的 `task_template` 全部使用 `format!()` 内联构建，每个包含完整任务描述、阶段特定说明、`finish_warning` 调用、上下文占位符。导致：
- `run_pipeline()` 方法异常庞大
- 修改模板需要重新编译
- 无法支持用户自定义流水线阶段
- 模板字符串中大量格式化参数，可读性差

**修复方案**：将阶段模板提取为外部文件（如 `pipeline-stages.toml`）或常量数组，使用 `include_str!()` 加载。**尚未修复**。

### 5. 六种身份共享的 `tool_usage_guide` / `output_guide` / `guard_guide` 内容重复

**位置**：`src/agent/identity.rs` 中 3 个关联函数，在 `system_prompt()` 末尾统一附加

**内容**：每个约 80-100 字，共约 300 tokens，在 6 个身份中完全重复。

**当前情况**：这是设计使然（每个身份是独立的 system prompt），但仍有优化空间：
- `guard_guide` 的"不要写中间文件"部分过于严苛（已通过 P0-2 修复缓解）
- 部分内容可考虑只由主系统提示词覆盖（如果子代理也能看到父系统提示词的话）

**修复方案**：当前设计合理，但可考虑：
- 精简通用指南文本（减少冗余表述）
- 非关键部分（如防死循环规则）可合并到主系统提示词中

### 6. Architect 缺少 `edit_file` 工具

**位置**：`src/agent/identity.rs` 中 `AgentIdentity::Architect.default_tools()`

**当前 Architect 工具集**：`read_file`, `batch_read_files`, `write_file`, `glob`, `list_directory`, `file_exists`, `exec_command`, `kb_store`, `kb_query`, `finish`

**缺少**：`edit_file`

**问题**：Architect 的工具集包含 `write_file` 但不包含 `edit_file`，无法精确定位修改已有文件（如修改设计文档、配置文件）。

**修复方案**：✅ **已修复** — 为 Architect 添加 `edit_file` 工具。

---

## 🟡 P2: 应改进

### 7. finish 决策树不完整

**位置**：`src/prompt.rs` 中"决定是否调用 `finish` 工具"部分

**当前决策树**：
```
用户输入
  ├── 明确要求执行任务（改代码/分析/搜索/重构）→ 完成后调用 finish(summary=...)
  ├── 只是提问/讨论/咨询意见 → 直接回答，不调用 finish
  └── 不确定是否是任务 → 先问用户"这是否算完成任务"
```

**问题**：
- "分析/研究"类任务（如"分析性能瓶颈"）既属于"分析"（列在第一条，应调用 finish）又可能被理解为"调研型任务"
- 缺少"多轮对话中完成子任务"的场景指导
- 决策树使用纯文本描述，LLM 可能忽略

**修复方案**：增加"分析/研究"的明确说明，增加"多步骤任务"的指导。**尚未修复**。

### 8. 测试断言过于宽松

**位置**：`src/skills/mod.rs` 的测试 `format_skills_for_prompt_handles_empty_list`

**当前代码**：
```rust
assert!(prompt.contains("暂无") || prompt.trim().is_empty() || prompt.contains("No skills"));
```

**问题**：这个断言过于宽松，只要满足任意一个条件就通过。实际上当前 `format_skills_for_prompt` 对于空列表返回空字符串，所以 `prompt.trim().is_empty()` 总是 true。但"暂无"和"No skills"的检查永远不会执行，给人一种错误的覆盖感。

**修复方案**：改为精确断言 `prompt.trim().is_empty()`。**尚未修复**。

### 9. 构建目录列表不完整

**位置**：`src/prompt.rs` 安全策略部分（第 2 条规则）

**当前列表**：`target/`、`node_modules/`、`.git/`、`dist/`、`build/`

**缺少**：
- `__pycache__/`（Python 字节码缓存）
- `.venv/`、`venv/`（Python 虚拟环境）
- `vendor/`（Go/Ruby 依赖）
- `.next/`（Next.js 构建产物）
- `.nuxt/`（Nuxt.js 构建产物）
- `*.pyc`（编译的 Python 文件）

虽然 code-review 技能中有更完整的语言特定表格，但主系统提示词中的安全策略列表是 Agent 的第一道防线，应该更全面。

**修复方案**：补充缺失的构建目录。**尚未修复**。

---

## 🟢 P3: 小优化

### 10. 流水线各阶段 finish 示例字符串硬编码

**位置**：`src/agent/mod.rs` 中 `finish_warning` 的 6 次调用

**示例**：
- `Self::finish_warning("架构设计", "架构设计完成：模块划分、接口定义、数据流、决策理由")`
- `Self::finish_warning("代码实现", "代码实现完成：新增文件、关键接口、测试覆盖")`

**问题**：虽然这些示例有用，但硬编码在 Rust 代码中导致修改措辞需要重新编译、国际化困难。

**修复方案**：将示例字符串提取为常量或外部配置文件。**尚未修复**。

### 11. 流水线模板中每个阶段重复包含完整任务描述

**位置**：`src/agent/mod.rs` 的 `run_pipeline()` 中 6 个阶段模板都包含 `{task_ref}`（原始任务描述）

**问题**：对于 5 阶段以上的流水线，后续阶段（如"问题修复"、"进度记录"）仍然包含完整的原始需求描述，而这些阶段只需要知道前一阶段的输出和当前阶段的任务即可。这在上下文紧张时是浪费。

**修复方案**：后续阶段模板中只引用 `{context}`（前一阶段输出），不再包含原始任务描述。**尚未修复**。

### 12. 子代理 system prompt 中缺少 `context_budget` 等工具的说明

**位置**：`src/agent/identity.rs` 中所有身份的 `default_tools()` 都不包含 `context_budget`、`compress_context`、`save_summary` 工具

**问题**：这些工具只有主 Agent 的 system prompt 中提到了，但子代理（尤其是深度嵌套的子代理）也可能需要上下文管理能力。当前子代理无法主动管理自己的上下文预算。

**修复方案**：考虑为子代理添加 `context_budget` 工具（至少），让子代理能感知自己的上下文使用情况。**尚未修复**。

---

## 已实施的修复

| 问题 | 优先级 | 状态 | 修改文件 |
|------|--------|------|---------|
| 双重标题 | P0 | ✅ 已修复 | `src/skills/mod.rs` |
| 技能矛盾指令 | P0 | ✅ 已修复 | `skills/code-review/SKILL.md` |
| General 提示词过简 | P1 | ✅ 已修复 | `src/agent/identity.rs` |
| Architect 缺少 edit_file | P1 | ✅ 已修复 | `src/agent/identity.rs` |

## 待修复的问题

| 问题 | 优先级 | 修复难度 | 建议方案 |
|------|--------|---------|---------|
| 流水线模板硬编码 | P1 | 高 | 提取为外部配置文件 |
| 共享指南重复 | P1 | 低 | 精简通用指南文本 |
| finish 决策树不完整 | P2 | 低 | 增加"分析/研究"、"多步骤任务"说明 |
| 测试断言宽松 | P2 | 低 | 改为精确断言 |
| 构建目录列表不完整 | P2 | 低 | 补充缺失目录 |
| 示例硬编码 | P3 | 中 | 提取为常量 |
| 模板重复任务描述 | P3 | 低 | 后续阶段不包含原始任务 |
| 子代理缺少预算工具 | P3 | 中 | 为子代理添加 context_budget |

---

## 建议立即修复的 P1 问题

### 流水线模板硬编码 (P1)

**建议方案**：创建一个 `pipeline-stages.toml` 配置文件，将 6 个阶段的模板移出 Rust 代码。

```toml
[[stages]]
name = "架构设计"
agent_type = "architect"
max_iterations_weight = 2
task_template = """
这是流水线第一阶段：架构设计。

原始需求：
{task_ref}

{context}

请使用 kb_store 将架构决策保存到 pipeline/stage-0/ 目录下。

{finish}
"""
finish_description = "架构设计"
finish_example = "架构设计完成：模块划分、接口定义、数据流、决策理由"
# ... 其他阶段类似
```

然后在 `run_pipeline()` 中加载配置文件，按权重分配迭代次数。

### finish 决策树改进 (P2)

**建议改进**：
```
用户输入
  ├── 明确要求执行任务（改代码/分析/搜索/重构）
  │   ├── 单步骤任务 → 完成后调用 finish(summary=...)
  │   └── 多步骤任务 → 完成所有步骤后调用 finish
  ├── 只是提问/讨论/咨询意见 → 直接回答，不调用 finish
  ├── 分析/研究类任务（如"分析性能瓶颈"、"调研架构"）
  │   ├── 需输出正式报告 → 完成后调用 finish
  │   └── 只需口头解释 → 直接回答，不调用 finish
  └── 不确定是否是任务 → 先问用户"这是否算完成任务"
```

### 构建目录列表补充 (P2)

**建议补充为**：
```
绝不读取 `target/`、`node_modules/`、`.git/`、`dist/`、`build/`、
`__pycache__/`、`.venv/`、`venv/`、`vendor/`、`.next/`、`.nuxt/` 等
目录中的文件（二进制产物/构建产物/依赖文件，非源代码）。
```