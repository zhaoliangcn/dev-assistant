# 记忆系统优化分析报告

## 一、记忆系统全景

当前项目的"记忆系统"由 **9 个组件** 组成，分散在多个模块中：

| 组件 | 文件 | 存储介质 | 用途 | 集成状态 |
|------|------|---------|------|---------|
| KnowledgeBase (KB) | `src/tools/kb.rs` | `.kb/` 目录 + Markdown + YAML frontmatter + `index.json` | 架构决策、接口定义、摘要等 | ✅ 通过 `kb_store`/`kb_query` 工具集成 |
| 分层摘要系统 | `src/agent/summary.rs` | `.kb/summaries/{session_id}/` | round/phase/final 三层摘要 | ✅ 已修复：自动触发集成 |
| 会话持久化 | `src/persist/mod.rs` | `.dev-assistant-store/session_*.jsonl` | 完整事件日志（JSONL） | ✅ 已修复：写入失败跟踪升级 |
| 会话日志 | `src/session/mod.rs` | `.dev-assistant-store/logs/.dev-assistant-session-*.log` | 可读的纯文本日志 | ✅ 已修复：移入子目录 |
| 检查点 | `src/orchestrator/checkpoint.rs` | `.kb/checkpoints/` | 任务状态快照 + 上下文重建 | ✅ 已修复：路径 bug |
| 上下文压缩 | `src/agent/compressor.rs` | 内存 | Truncate/Summarize 压缩历史 | ✅ 集成在 Agent step 循环 |
| 上下文预算 | `src/agent/context.rs` | 内存 | 跟踪 token 使用率 | ✅ 已修复：tool_schema_tokens 自动设置 |
| Pipeline 上下文 | `src/agent/pipeline_context.rs` | `.kb/pipeline/` | 流水线阶段上下文 | ✅ 集成在 Agent 中 |
| 文件缓存 | `src/tools/cache.rs` | 内存 | 文件读取缓存 | ✅ 集成在工具中 |

---

## 二、问题修复状态

### ✅ 已修复 (3 Critical + 4 High/Medium)

| 问题 | 级别 | 修复内容 | 文件 |
|------|------|---------|------|
| C1. CheckpointManager 路径 Bug | 🔴 Critical | `TaskOrchestrator::new` 传入 `.kb` 而非 `.kb/checkpoints` | `src/orchestrator/mod.rs:122` |
| C2. SessionStore 静默数据丢失 | 🔴 Critical | 新增 `consecutive_write_failures` 跟踪，≥3 次升级为 error 级别 | `src/persist/mod.rs` |
| C3. 分层摘要未自动集成 | 🔴 Critical | 上下文压力达 Critical/Exhausted 时压缩前自动保存轮次摘要 | `src/agent/mod.rs` |
| H2. 日志文件根目录污染 | 🟠 High | 会话日志移到 `.dev-assistant-store/logs/` 目录 | `src/session/mod.rs` |
| H3. memory_tokens 未设置 | 🟠 High | 移除 `unused` 的 `set_memory_tokens` 方法 | `src/agent/context.rs` |
| M5. tool_schema_tokens 未设置 | 🟡 Medium | 在 `step()` 中每次 LLM 调用前自动计算并设置 | `src/agent/mod.rs` |
| H2. `.gitignore` 日志覆盖 | 🟠 High | `*.log` 已在 `.gitignore` 中，新路径仍在 `.dev-assistant-store/` 下（已 ignore） | `.gitignore` |

### ⏳ 待处理 (遗留问题)

| 问题 | 级别 | 说明 | 难度 |
|------|------|------|------|
| H1. SessionStore/SessionLogger 重叠 | 🟠 High | 两个系统并行记录同一会话，需重构统一 | 大 |
| H4. 检查点恢复未集成 | 🟠 High | `rebuild_context_from_checkpoint` 从未被调用 | 中 |
| H5. KB 搜索算法原始 | 🟠 High | 无分词、无模糊匹配、无全文搜索 | 中 |
| M1. ReadCache 代码重复 | 🟡 Medium | read/write 同步异步方法几乎完全重复 | 小 |
| M2. SummaryStore 目录遍历 | 🟡 Medium | 每次加载遍历整个目录，O(n) 性能 | 小 |
| M3. KB 索引全量写入 | 🟡 Medium | 每次 `kb_store` 全量序列化写入 index.json | 中 |
| M4. SessionStore dead_code | 🟡 Medium | `path()`/`session_id()`/`read_events()`/`list_sessions()` 无人使用 | 中 |

---

## 三、修改摘要

### 1. `src/orchestrator/mod.rs` — C1 路径修复

```diff
- checkpoint: CheckpointManager::new(&kb_root.join(".kb/checkpoints")),
+ checkpoint: CheckpointManager::new(&kb_root.join(".kb")),
```

### 2. `src/persist/mod.rs` — C2 写入失败跟踪

新增 `consecutive_write_failures: u32` 字段，写入失败时累计计数：
- 1-2 次失败：`tracing::warn!`
- ≥3 次连续失败：`tracing::error!`（提示可能磁盘已满或权限变更）

### 3. `src/agent/mod.rs` — C3 自动摘要 + M5 tool_schema_tokens

**自动摘要**：在 `step()` 循环的工具调用处理分支中，压缩上下文前检查预算压力：
- Critical/Exhausted → 自动调用 `auto_save_round_summary()`
- 抽取 `save_layered_summary()` 方法，供 save_summary 工具和自动摘要共用
- 新增 `auto_save_round_summary()` 和 `build_auto_summary()` 方法

**tool_schema_tokens**：每次 LLM 调用前，计算工具 schema 的 token 数并设置到 budget 中。

### 4. `src/session/mod.rs` — H2 日志目录迁移

日志文件从项目根目录移到 `.dev-assistant-store/logs/` 目录：
- 创建目录：`{working_dir}/.dev-assistant-store/logs/`
- 文件名不变：`.dev-assistant-session-{YYYYMMDD-HHMMSS}.log`

### 5. `src/agent/context.rs` — H3 清理 dead code

移除 `ContextBudgetManager` 和 `ContextManager` 上的 `set_memory_tokens` 方法（从未被调用，记忆注入机制尚未实现）。

---

## 四、验证状态

```
cargo check: ✅ 0 errors
cargo test:  ✅ 351 passed, 6 failed (6 个失败均为预存的 tools::file::symbol 测试问题，与本次改动无关)
```

### 遗留警告 (8 个)

均为 `dead_code` 或 `never_used` 警告，涉及：
- `LayeredSummaries` 结构体（供 `rebuild_context_from_checkpoint` 使用，但后者未集成）
- `SummaryStore` 的 `session_id`/`root`/`load_final`/`load_all`（同上）
- `CheckpointManager` 的 `kb_root` 字段（仅供 `rebuild_context_from_checkpoint` 使用）
- `rebuild_context_from_checkpoint` 方法本身（H4，未集成到 execute 循环）
- `should_compress` 方法（设计为预算检查用，但当前由 Agent 自动触发）

这些警告在 H4 修复后自然消除。